namespace Observer
{
    class Program
    {
        static void Main(string[] args)
        {
            // An observer notifies all listeners when the observed object changes.
            // example: events, IObservable or INotifyPropertyChanged
            // https://en.wikipedia.org/wiki/Observer_pattern
        }
    }
}
