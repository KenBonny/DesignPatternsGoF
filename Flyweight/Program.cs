namespace Flyweight
{
    class Program
    {
        static void Main(string[] args)
        {
            // https://en.wikipedia.org/wiki/Flyweight_pattern
            // used to conserve memory by sharing a resource: icon that is used multiple times.
            // Load it once, add reference in multiple objects.
        }
    }
}
